<!DOCTYPE html>
<html lang="en" class="scroll-pt-[calc(var(--hk-nav-height)+var(--spacing)*7)] theme-<%= theme %>-light">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hanakai contributors</title>
    <%= favicon_tag %>
    <%= stylesheet_tag "app" %>
  </head>
  <body>
    <%= render("header/base") %>

    <div class="container mx-auto mt-4 content">
      <%= yield %>
    </div>

    <%= javascript_tag "app" %>
  </body>
</html>
