<nav class="flex items-center space-x-2 text-gray-500 text-sm mb-4" aria-label="Breadcrumb">
  <%= link_to "Home", routes.path(:root) %>
  <span class="text-gray-500">/</span>
  <%= tag.span(organization.name) %>
</nav>
